import 'package:flutter/material.dart';

class RenderMainMenu extends StatelessWidget {
  RenderMainMenu({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Main Menu'),
    );
  }
}
